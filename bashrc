# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi
#csh
#source ~/.aliases
#setenv SGE_LONG_JOB_NAMES -1
#setenv DEBUG_DEV
#set path = (/users/ddon//.config/sublime-text-3/Packages/PerlTidy/perltidy $path)
#/nas/projects/ipcad/iptools/bin/validate_svn_cfg.sh
# Uncomment the following line if you don't like systemctl's auto-paging feature:
# export SYSTEMD_PAGER=

# User specific aliases and functions
